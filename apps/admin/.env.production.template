# Production Environment Variables Template
# Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to version control

# Database Configuration
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/leetour

# Authentication
# Generate a strong, unique secret key for JWT tokens
JWT_SECRET=generate_a_strong_random_secret_key_here_at_least_32_characters

# Environment
NODE_ENV=production

# Optional: Add your production domain for CORS if needed
NEXT_PUBLIC_APP_URL=https://your-app-domain.com

# Instructions:
# 1. Copy this file: cp .env.production.template .env.production
# 2. Replace all placeholder values with your actual production values
# 3. Generate a strong JWT_SECRET using: openssl rand -hex 32
# 4. NEVER commit .env.production to git (it's in .gitignore)